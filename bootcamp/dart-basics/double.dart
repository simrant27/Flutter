void main() {
  double score = 55.55;

  print(score);

  print("Asmu aunty scored " + score.toString() + " % in SEE");

  var opacityOfBox = 0.34;
  print("A box is " + (opacityOfBox * 100).toStringAsFixed(0) + "% visible");

  print(
      "Type of opacityBox variable is: " + opacityOfBox.runtimeType.toString());

  double height = 5;
  height = height * 0.2;
  print("height is" + height.toString());
}
