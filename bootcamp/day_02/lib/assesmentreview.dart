import 'package:flutter/material.dart';

void main() {
  runApp(const Review());
}

class Review extends StatelessWidget {
  const Review({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: const Text("fdgf")),
      ),
    );
  }
}
